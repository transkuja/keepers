L'action de finir le jeu s'affiche trop haut (et il manque le sprite)

Lors du restart, les positions des monstres peuvent être mal initialisées dans le TileManager (le bug est très random)

ResolveBattle appelé en cascade pour chaque mesh collider